# Copyright 2024 Canonical Ltd.
# See LICENSE file for licensing details.
# This file populates the Overview on Charmhub.
# See https://juju.is/docs/sdk/metadata-reference for a checklist and guidance.
name: smtp-relay
display-name: SMTP relay
summary: SMTP server configured to allow SMTP relaying.
docs: https://discourse.charmhub.io
issues: https://github.com/canonical/smtp-relay-operator/issues
maintainers: 
  - https://launchpad.net/~canonical-is-devops
source: https://github.com/canonical/smtp-relay-operator
description: |
  A single sentence that says what the charm is, concisely and memorably.

  A paragraph of one to three short sentences, that describe what the charm does.

  A third paragraph that explains what need the charm meets.

  Finally, a paragraph that describes whom the charm is useful for.
# provides:
#   grafana-dashboard:
#     interface: grafana_dashboard
#   metrics-endpoint:
#     interface: prometheus_scrape
requires:
  certificates:
  interface: tls-certificates
  limit: 1
  description: Requires certificates-generating charm for TLS
#   logging:
#     interface: loki_push_api
#     limit: 1
#     optional: true
  milter:
    interface: milter
peers:
  smtp-relay-peers:
    interface: smtp-relay-instance
